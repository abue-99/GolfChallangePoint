/**
 * Auth components barrel export.
 *
 * Place auth-specific UI components here (e.g. login form, signup form).
 * Re-export them from this index for convenient imports:
 *   import { LoginForm } from "@/components/auth";
 */

// Future auth component exports will be added here.
// Example:
//   export { default as LoginForm } from "./LoginForm";
//   export { default as SignupForm } from "./SignupForm";
