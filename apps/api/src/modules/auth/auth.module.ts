import { Module } from "@nestjs/common";

/**
 * Auth module – handles authentication (login, signup, password reset).
 * TODO: Add AuthController and AuthService.
 */
@Module({})
export class AuthModule {}
