{
  "name": "challangepoint-mvp",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm --filter @golf/db run generate && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "start": "pnpm --filter ./apps/web start",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prisma": "^7.5.0",
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "@prisma/client": "^7.5.0"
  }
}